import XCTest
@testable import VariableBlurView

final class VariableBlurViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VariableBlurView().text, "Hello, World!")
    }
}
